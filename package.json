{
  "name": "school-app",
  "version": "1.0.0",
  "description": "School Timetable Application",
  "scripts": {
    "start": "http-server ./ -p 8080 --cors -c-1",
    "dev": "nodemon --watch src --exec 'npm start'"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "nodemon": "^2.0.22"
  }
}
